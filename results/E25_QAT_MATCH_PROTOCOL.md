# E25 — matched QAT versus accepted float at16000 updates

Registered before implementation/scientific training,2026-09-07. User approved the proposed three-seed QAT comparison. Root owns design/shared docs/results; one bounded executor and a separate Astra/low reviewer. Following repeated serious Luna/xhigh runner failures in E24, the narrow new runner is assigned Astra/low under the AGENTS escalation rule; no inherited full-history fork or broad research sweep.

## Hypothesis and comparison

Does the existing BitLinear QAT package retain the registered task level at the successful float training budget? Change only the14 registered core/readout Linear projections to existing BitLinear execution: ternary fake-quantized weights plus per-vector INT8 fake-quantized activations, FP32 masters/STE/bias unchanged. This is the whole quantization package, not a weight-only causal claim. Signed-bit input projections, norms, embeddings, reader/state/instruction logic and native8 remain identical,151232 parameters. No packed weights/kernel/memory/speed benefit is claimed.

Train new QAT models from initialization for seeds0/1/2, exactly16000 updates each. Compare with frozen accepted E24 float u16000 results/checkpoints, not post-hoc quantized trained float. No float retraining if initial/stream/update equivalence checks pass. E20/E22/E24 historical float chain already verified full optimizer/RNG continuation and evaluation immutability; freeze all provenance. If equivalence cannot be established, stop before training and report the concrete mismatch rather than silently changing comparator.

## Paired initialization and fixed training

For each seed, construct the same QATRegisterModel master used by E22, replace only learned numeric embeddings with E17 SignedBitsEncoder projections (local generator seed=s, x then y, each shape64x4, randn*0.01), and use accepted NativeStepRegisterModel.from_e17_bits(...,8). Retain exactly14 original BitLinear locations. Compare every named parameter and state tensor, key/order/shape/dtype with accepted E22 build_initial_model(s) float initializer using torch.equal, with separate storage. Seed0 additionally matches saved E18/E20 initial state; seeds1/2 match E22 saved initials. QAT and float computational outputs need not match; their FP32 initial masters must.

Freeze and restore the ACTUAL historical float training-initial torch RNG for each QAT arm after all constructors so stochastic state is paired too. For seed0 this is torch.manual_seed(0) immediately after E20 runner seed_everything(0): its initial factory forks RNG and preserves the caller state. It equals the stored E20u8000/E24seed0u16000 RNG; do NOT use E22 factory seed0 post-constructor RNG, which differs. For seeds1/2 use the saved E22 initial RNG that its actual runner explicitly restores. This compensates differing incidental constructor RNG consumption; no parameter changes. Root reference results/E25_INITIAL_REFERENCE.json specifies exact digests. Original E22 constructor audit is preserved separately in E25_INITIAL_REFERENCE_CONSTRUCTOR_AUDIT.json. This clarification was made before preflight, scientific training or any scientific forward. All training CPU4 deterministic FP32, no autocast, fixed AdamW and clipping1, accepted E20 _update with loss after every instruction, batch64, native8.

Use exact accepted E20 base2000 ordered batches eight times, data seed0. Verify stream/target hashes and every repeat; no shuffle, resampling, curriculum, LR schedule or optimizer reset. One continuous AdamW per QAT seed, exactly16000 updates. Total48000updates/3072000examples/49152000 recurrent training substeps. Progress/loss every250 updates only; no intermediate scientific evaluation or model selection.

## Final evaluation and predicates

Save one final u16000 QAT checkpoint per seed with complete model/optimizer/RNG and frozen initial/config/source/data/float-comparator provenance. Fully evaluate each final regardless of earlier completed numerical failure: seen32 programs on train192/validation32; E21 six primary plus separate equivalent control all256 states, exact intermediate instruction traces and train/validation/test initial-state strata. No unseen longer programs. Evaluation budget213forwards/26880cases/70464readouts/563712 substeps, excluding independent replay.

Same thresholds: each primitive32/32; each seen composition>=31/32 (seen prerequisite). Each of six primary programs>=244/256 (primary conjunction). Report separately and together for each seed; task-level retention success requires ALL THREE combined conjunctions. Primary-all-seeds also reported separately. Do not invent a post-hoc noninferiority margin or call a nonsignificant difference equivalence. Report QAT-minus-float per-program integer counts/rates, fulltrace/boundary and seen deltas, paired wins(QAT correct/float wrong), losses(QAT wrong/float correct), bothcorrect/bothwrong on identical program/state/target/strata. Control excluded from primary. No gates prevent reporting QAT composition results after seen failure; these are diagnostic counts, not a claim that primitives generalized.

If QAT fails while matched float passes, conclude this fixed BitLinear package/training recipe did not retain task level. This does not prove QAT cannot work after different optimization, nor isolate weight versus activation quantization. Both conditions on fixed data and opened E21 set, adaptive architecture/budget history, only three initialization seeds.

## Integrity, tests, failures and stop

Write only new looped_bitnet/qat_match_e25.py, scripts/qat_match_e25.py, tests/test_qat_match_e25.py, results/E25_* and fresh runs/e25_qat_match_preflight/ plus runs/e25_qat_match/. Preserve all old source/runs/guards. Reuse accepted pure evaluation/paired helpers where safe; no monkeypatching old constants or scientific runs through private QA modes. Freeze protected/reference hashes now and new source/protocol hashes in canonical preflight after code CLEAR. Root independent initializer reference before updates.

Actual tiny three-seed runner must train/evaluate real QAT models on legal seen-only examples, save/strict-reload/next-update identity and actual costs; no fake evaluator claiming forwards. Check14BitLinear inventory, FP32 master pairing, finite gradients and QAT checkpoint reload. Verify numeric predicate false continues all seeds; any exception/nonfinite suspends with actual completed updates/forwards and preserved artifacts. Check metadata/seed/update/QAT inventory tamper, target/state/stratum/paired marginals,244vs243/control separation, overwrite refusal. Keep QA counts separate including failed attempts; no broad inherited suite unless needed. Independent code CLEAR before scientific run, then separate full result replay/recount/provenance review. No automatic retraining after technical failure.

Stop all three fixed finals and independent review irrespective numeric outcome; no additional budget/seed selection/QAT variants/length tests/cloud or E19 activation. Update HANDOFF/RESEARCH_LOG/ROADMAP. Automation PAUSED.
