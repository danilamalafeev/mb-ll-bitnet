# E32 — paired W4/float width128 with fixed FFN256

2026-09-08. User authorized paired width128 training after E31 and explicitly retained FFN256. Root owns hypothesis/protocol/references/shared documents. Luna/xhigh owns implementation, fixed execution, mechanical tests, artifacts and report assembly; separate Astra/low reviews design/code/results. No inherited implementation escalation. This protocol must receive design CLEAR and source code CLEAR before canonical preflight/science.

## Hypothesis and four comparison cells

Test whether increasing width improves ordinary continuous L5 execution in W4 and float under the same training recipe. New W4-128 and float128, seeds0/1/2 each, from scratch16000updates. Historical W4-64=E27 and float64=E24 u16000 are frozen comparators; E28 supplies their ordinary L4/L5 predictions, E27/E24 their seen/E21 predictions. Verify historical data/schedule/optimizer/initial-pair/checkpoint provenance before treating them as controls. Historical float continuation is admissible through accepted exact optimizer/RNG lineage and uninterrupted-equivalence checks, not an optimizer reset. Do not retrain64 merely for symmetry. No presumption that W4 is worse than float: prior L5 errors are W4 68/21/62 versus float138/6/64.

Sole architecture intervention: hidden/model width64→128, related input/role/opcode embeddings, LayerNorms, reader projections and output heads adapt to128. FFN inner dimension remains256, blocks4, heads4, native8 internalsteps PER instruction. Head dimension therefore16→32, with existing attention scaling formula; this is a width-package test, not h-only state capacity isolation. Preserve native opcode-addition coefficient exactly1/8 (old literal1/sqrt64); do not silently change it to1/sqrt128. Parameter count is151232 at64 and335232 at128 (2.216697x), independently derived as4*d*d+2107*d with fixed FFN256; verify against actual model inventory before training. W4-eligible matrix weights331776 and remaining FP32 parameters3456 at128; float has all335232FP32. These are logical counts, not packed storage. FFN expansion ratio changes4→2. Same bit input encoding values and initialization distributions, fixed initial input-derived KV, shared reader, block order, loss and continuous carried h. No decoded feedback, E31reset, scratchpad, extra loops or wider FFN.

W4 uses exactly E27 symmetric matrix absmax/7 scale clamp1e-8, round/clamp[-7,7] identity STE at the14 reader/FFN/head projections. Float uses the matching FP32 linear operation. Signed-bit input projections, embeddings, norms and biases remain FP32. State/cache/residual/activation and all master weights/optimizer FP32, CPU4 deterministic, autocast disabled. No packed INT4 or hardware speed/memory claim.

## Initialization and training match

For each128seed construct fresh weights from manual_seed(seed) with the same constructor/init ordering generalized to128 and fixed FFN256. Signed-bit input projection weights use a private generator seeded with the same seed and Normal(0,.01) at shape128x4. W4 and float within128seed must have exactly identical named FP32 initial tensors, ordering/shapes/dtypes, disjoint storage, and training-start Torch RNG; save initial state+RNG and digest before updates. Reuse the historical64 accepted actual training-start RNG for that seed explicitly after constructor calls (seed0 historical manual_seed0, seeds1/2 saved accepted RNG). Across widths, tensors/shapes necessarily differ; nominal seeds are matched initialization recipes, not identical initial parameters. Verify width64 version of new factory against accepted historical initial masters and actual RNG, no science needed. Record all changed dimensions and actual parameter counts and separate quantized/unquantized weight counts before training.

Same E20 base2000 ordered batch64 schedule repeated8, data seed0, all targets/stream hashes identical; same AdamW hyperparameters, clipping1 and existing instruction-average CE(x)+CE(y) update. No teacher forcing. Every one of6models gets16000updates, no intermediate scientific evaluation or checkpoint selection; only loss/progress every250updates. Train six models sequentially on local CPU, fixed order seeds0/1/2 each float then W4 (declare labels exactly in manifest). Numerical low-accuracy outcomes do not stop later models. Technical exception/nonfinite suspends with accurate attempted/completed counters and preserved artifacts; no automatic restart/retraining. Final checkpoint u16000 only for science with optimizer/RNG/mode/config/initial/stream/source/provenance. QA uses private tiny budgets and separate paths.

Scientific training budget:96000updates/forwards,6144000examples,12288000instruction readouts,98304000internalsteps. Equal updates/exposure, not equal FLOPs or walltime. Historical64 training cost is imported, not executed/counted again. No CUDA/cloud/paid compute, optimization sweep, extra training or automation activation.

## Fixed final evaluation and criteria

Evaluate all6fixed finals on original seen32programs train192/validation32; E21 six primary compositions plus separate equivalent control over256states with original state strata; frozen E28 six L4 and six L5 programs each all256states. Exact original continuous execution, native8 perinstruction; no oracle/reset. E21 primary is the existing composition benchmark (not relabeling every program as exactly L3). Freeze all program identities/targets/strata before training, no selection changes.

Per new model evaluation:83forwards,12032cases,37312readouts,298496internalsteps. Total498forwards,72192cases,223872readouts,1790976steps. Always evaluate all scopes even if seen fails. Independent replay/QA separate costs.

Primary length restoration perprecision arm: FINAL-readout joint correctness for each of6L5programs >=244/256 in every one of3seeds. Report seen prerequisite (primitive validation32/32; each seen composition>=31/32), E21 primary each>=244/256 excluding equivalent control, and L4 each>=244/256 separately. Combined success requires all these prerequisites and primary L5 across all3seeds. Secondary fulltrace correctness has the same >=244/256 eachprogram/all3seeds conjunction, reported separately for L4 and L5 and never substituted for primary. Preserve firstdivergence/recovery histograms independently; a correct readout is not proof correct latentstate. No average substitution or seed replacement.

Descriptive width benefit: strict decrease of L5 final error total in EACH seed relative to that precision's frozen64 model; report predicate separately from restoration and actual deltas, even if mixed. Perprogram paired newwins/oldwins/bothcorrect/bothwrong for final AND fulltrace on identical original program/state/target/stratum, plus seen/E21 deltas. Report W4 vs float at128 and imported64 contrasts. For each seed and length, error_gap(width)=errorsW4(width)-errorsFloat(width), interaction=gap128-gap64; negative interaction means relative W4 error gap decreased here, not causal proof quantization losses are compensated. Show signed values, no posthoc margin/significance/independent-trial claim. Individual cases across seeds/functions repeated and opened; no novel holdout claim. Do not infer memory bottleneck from width benefit, or disprove it from a negative result.

Save full raw predictions for every evaluated scope, original targets/state strata and keyed case identity. Verify DSL, row coverage/duplicates/thresholds independently. Longer program novelty is inherited frozen E28 selection, only6functions perlength; length and program content not isolated. No arbitrarydepth or language reasoning claim.

## Integrity, QA and review

Write only new looped_bitnet/width_e32.py, scripts/width_e32.py, tests/test_width_e32.py, results/E32_* and unique runs/e32_* paths. Old code/runs/checkpoints/guards immutable. Root freezes E32_REFERENCE and E32_PROTECTED_HASHES extending E31 and referencing historical64 outputs/initials and current protocol before science. Code hashes and exact initial/stream/parameter inventory frozen in canonical preflight only after independent CLEAR. Do not monkeypatch old constants/scientific evaluators or existing modules globally.

Targeted checks: width64 factory initial-master identity;128shapes/parameter inventory/heads/FFN/opcode scale; W4 exact14locations and float replacement; paired masters/storage/RNG; stream/optimizer match; save/reload strict payload validation, wrong arm/width/seed/update/config/source/initial/RNG/optimizer rejection and exact next-update identity including optimizer/RNG; ordinary carry path no reset; metrics joins/strata/243-244 boundaries/control exclusion; failure preserves actual counters/partial report; overwrite refusal and protected hashes. Use tiny legal-seen ADD/at most two training updates per6cells for real runner QA, plus separately counted one-next-update uninterrupted/reloaded controls; no scientific E21/L4/L5 checkpoint predictions before code CLEAR. All QA attempts and failed test-driven model work disclosed; don't claim fabricated clocks or counts. Source/test changes after QA documented; final reviewed hashes precede science.

Final separate review: strict savedfinal reload for all6models, complete fixed498forward evaluation replay, exact predictions/metrics/report comparison excluding declared timing/path fields, independent raw DSL/joins/counts/strata/gates/interactions and checkpoint/optimizer/RNG/initial/stream/provenance audit, source/protected hashes unchanged. No96k-update retraining for review. Stop after fixed six finals and independent ACCEPT or genuinely blocked result plus HANDOFF/RESEARCH_LOG/ROADMAP/state update. No E33 or automatic width256, FFN512, reset, scratchpad, budget extension, E19 or cloud.
