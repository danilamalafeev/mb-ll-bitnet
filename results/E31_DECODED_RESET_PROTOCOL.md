# E31 — own decoded fourth state, fresh fifth operation

2026-09-08. User authorized this diagnostic before width-128. Root owns protocol/reference/shared docs. One narrow Astra/low executor under the documented E27 implementation escalation, separate Astra/low reviewer. No training, new architecture, precision change, longer programs, cloud or automation.

## Question and fixed scope

Does a fresh last-operation execution from the model's OWN decoded fourth readout improve the original L5 final answer without an oracle numeric input? Use saved E27 W4/A32 and E24 float u16000 checkpoints, seeds0/1/2, native8 for the fresh instruction. All six frozen E28 L5 programs, all256 original initial states per program/model/seed; do not select error cases. Reuse frozen E28 prefix predictions without rerunning training or the original trajectory.

For each original program/state case, use saved predicted_trace[3] as the two numeric inputs to a new one-op forward for that program's fifth opcode. This rebuilds input embeddings, cache and latent context. The DSL true fourth state is used ONLY for scoring and oracle comparison, never as the own-reset forward input. Direct batch256 forward once per program and model/seed; duplicate decoded numeric inputs are permitted and retain separate original case identities. Exactly36 scientific forwards,9216 model cases/readouts,73728 internal steps,0updates. This count concerns fresh suffix work only; saved four-instruction prefix work is reused, not newly executed. E30 predictions are controls, not substituted for these direct forwards.

## Identity, outputs and comparisons

Validate frozen E28 selection, full saved trace/target/stratum schema and keyed program plus original state coverage; retain original initial-state train/validation/test strata (192/32/32 per program). Validate each fresh output's input numbers and one-op target against the requested decoded input before assigning it back to the immutable original case identity. Duplicate decoded inputs must never collapse original cases. Compare direct fresh predictions with the E30 primitive lookup keyed by model/seed/fifthopcode/DECODEDstate; expose any discrepancy and do not silently substitute lookup results.

Save original program/name/state/stratum, original true and predicted traces, true fourth numbers, own decoded fourth numbers, fourth_readout_correct, prefix4_all_correct, original long-final correctness, fresh input, fresh predicted final, fresh local target and local correctness, own-reset correctness against ORIGINAL final target, hybrid full-trace correctness (original first4 readouts plus fresh fifth), E30 oracle fresh prediction/correctness and lookup consistency. The local one-op target may differ from the original desired final; do not confuse local accuracy with successful program execution.

Report paired recovered/introduced/bothcorrect/bothwrong and marginals, with rescued_fraction=null for empty old-error denominators. Break down by all cases, fourth-readout-correct/wrong, and all-first4-correct/wrong separately; original train/validation/test, program and model/seed, plus aggregate. Also compare own-reset versus oracle correctness. Count cases with previously wrong prefix but correct fourth readout explicitly.

## Prospective diagnostic predicates and interpretation

Primary late_error_rescue_all: in every model/seed's prefix4-all-correct subset, all old final errors are repaired and no old correct final becomes wrong; aggregate old-error denominator must be nonzero, per-cell zero denominators reported honestly. Report direct_lookup_match_all separately as a consistency check. Report own final correctness equivalence to fourth_readout_correct as a predicted diagnostic property, not an independent discovery: E30 already established exact fresh XOR/SWAP across the entire domain, and each last operation is bijective. For fourth-correct cases the E30 oracle and own inputs are identical. For fourth-wrong cases exact bijective last operations cannot restore the original correct final; a lost old recovery is possible.

Per-program final >=244/256 and hybrid fulltrace >=244/256, conjunction across all6 programs/all3 seeds separately per model, are descriptive gates for THIS changed execution only. Original E28 gates remain unchanged. Preserve all outcomes even if the proposed reset fails or introduces errors. No new length-generalization, hidden-width bottleneck, cache-only cause, or universal self-correction claim. This is a single discrete decode/re-encode boundary at position4, not reset after every instruction and not learned scratchpad. Scope is finite256state DSL and six previously chosen L5 functions, three seeds/model, no novel independent sample.

## Integrity, verification and stop

Write only new E31 code/tests/results and fresh exclusive runs/e31_* paths; preserve prior source, runs, checkpoints, manifests and references through a frozen protected SHA256 map. Freeze protocol, references and runtime source hashes before science. Independent code CLEAR and targeted tests before canonical preflight/evaluation. Tiny seen ADD QA on two states per each of6models allowed separately:6forwards12cases/readouts96steps,0updates per pass. Pure synthetic tests may exercise other opcodes but no early scientific checkpoint predictions. Tests should cover own-vs-oracle input, duplicate decoded numbers, local-vs-original target distinction, fourth-vs-prefix strata, full case scope, malformed data, empty denominators, overwrite refusal and failure accounting/mode preservation.

Record attempted/completed actual hooks, model/optimizer/RNG/all module modes unchanged, numerical/nonfinite/exception failure preserved with partial artifacts and no automatic retry. A legitimate negative numerical result is complete, not a technical failure. Separate elapsed time and actual counters for implementation QA, science and independent replay; no fabricated timing. Final independent saved-checkpoint replay of the fixed36forward scope plus raw stdlib recomputation of DSL, own input identity, target distinctions, paired counts/all strata, gates, manifests and source/protected hashes. Stop after fixedscope and independent review plus HANDOFF/RESEARCH_LOG/ROADMAP/workflow state update. Width-128 remains a candidate, not authorized by this E31 execution.
