# E37/E38 — fixed padding and seed replication

Registered 2026-09-08 before scientific execution. User authorizes these two checks, then STOP and discuss. Research design/independent review: followup_design Astra/low; fixed implementation/execution: followup_impl Luna/xhigh; root alone writes shared coordination documents. Scientific execution requires frozen manifest/source hashes and independent CODE CLEAR. Preserve accepted E32–E36 artifacts. No model selection, result-dependent tuning, additional seeds, new evaluation discovery, branch C, architecture or routing experiment.

## Frozen inheritance

Use accepted E36 v2 manifest `runs/e35_e36_preflight_v2/manifest.json` (SHA256 `4adf2dd1bb1036b94bcbf7a0246d9410596a737c9d20e5ff8865632fab5988aa`) as the exact source of program lists, training/heldout initial-state lists, A/B schedules, stream contents, evaluation categories and own-branch probes. Reuse its data seed0 for every model seed. Verify exact equality/digests before execution; do not regenerate alternative pools. Model initialization seeds1/2 come from their own accepted E32 checkpoints. E36 seed0 is already observed evidence; E38 replicates across two additional trained initializations on the SAME opened finite evaluation pool, not a new blind test.

Inherit both disclosed E36 scope clarifications: `train_syntax` labels union exposure from ancestor/A/B and is not branch-A seen syntax at L4–6; the separate historical six-L5 regression set is omitted from E38 branch evaluation. Parent evaluation below includes its original E32 L5 scope for lineage context only. Exact semantic-heldout exclusion remains union of all trained prefix functions (94), with 24 primary strings representing23 functions. Initial192/64 strata do not assert unseen intermediate states.

## E37: exact E35 padding on six E36 seed0 endpoints

Load all accepted float/W4 checkpoints A/u40000.pt, B_match/u36572.pt and B/u40000.pt from `runs/e35_e36_length_wave/science/{float128_seed0,w4128_seed0}/` through the unchanged strict E36 loader and its accepted manifest, without bypassing identity/provenance checks. Zero training. Use EXACT E35 programs: P=(ADD,ADD),(XOR,SWAP),(SWAP,XOR); O=ADD/XOR/SWAP; insert k=0,1,2,3 SWAP→SWAP pairs between P and O. Lengths3/5/7/9,36 programs per checkpoint, all256 initial states. Verify full-domain function and true pre-O state identity within families before forwards.

Save complete decoded traces and DSL targets. Within each checkpoint reproduce E35 descriptive final and full-trace paired counts of k>0 versus k0, with every denominator. Preserve supplemental final-O accuracy conditional on BOTH paired pre-O readouts correct, and report the excluded count. Across A versus B_match and A versus B, report paired final/full-trace recovered/introduced/both-correct/both-wrong per identical padded program/state and length, and padding degradation counts per branch. No threshold or success gate is newly selected; comparisons are descriptive, including losses at k0. B_match contrasts equal training instructions; B contrasts equal added updates. These identities alter history composition and inference compute jointly; improvement does not isolate hidden state, KV, reader, or prove arbitrary-chain execution.

## E38: own-parent preparation, then exact E36 replication

Four accepted own E32 parents are `runs/e32_width/{float128_seed1,float128_seed2,w4128_seed1,w4128_seed2}/u16000.pt`. Each receives exactly16000 additional E33-style updates to absolute32000, preserving its own full model/optimizer/RNG and unchanged width128/FFN256/head4/native8, batch64, loss, AdamW/clipping, W4/float settings and actual deterministic CPU4. Use the same frozen E20 base2000 stream repeated8 times, absolute batch indexing and stream/target digests as E33. No optimizer reset, weight transplant from seed0, training from scratch, intermediate evaluation or result-driven exclusion. Strict typed checkpoint loaders must validate seed1/2 identities and absolute optimizer steps; use new schemas/adapters rather than weakening accepted E33/E36 loaders or using QA bypasses.

Evaluate each new u32000 parent exactly once with unchanged E32/E33 endpoint scope:32 seen programs on train192/validation32, six E21 primary plus its separate equivalent control, six L4 and six L5 on all256 states. This confirms and records parent behavior/provenance; report original gates and own-parent paired deltas descriptively. It is not a new scientific eligibility filter: all valid finite parents continue irrespective of accuracy. If inherited continuation automatically performs that evaluation, consume that output and do not evaluate twice.

From each own new u32000, clone A/B with identical full optimizer/RNG. A gets8000 updates, schedule(1,2,3)*2666+(3,3),16002 batch-instructions. B gets8000 once, schedule(1,2,3,4,5,6)*1333+(1,4),27998 batch-instructions; save B4572 inside that SAME trajectory, not a separately trained prefix. B4572 has16002 batch-instructions. A ancestral short-repeat stream, B common first4000 short batches and long pools29/32/32 at L4/5/6 remain identical to E36. Save strict A absolute40000, B_match36572, B40000; evaluate snapshots only after the B run completes. No branching based on results.

Each of12 endpoints receives the unchanged E36 evaluation and branch-specific attainment probes exactly once. Primary per precision and seed: strictly fewer B full-trace errors than A on common allowed semantic-heldout L7–10, separately for B4572/A8000 and B8000/A8000; pool6144 cases,1536 per length. Report every error count/denominator, length, initial-state stratum, paired improvement/regression and full-trace/final metrics. Replication passes only if BOTH comparisons meet this directional criterion for BOTH precisions in EACH of seeds1/2; separately state conjunction with accepted seed0. A zero-error baseline cannot strictly improve. No averaging may replace these individual checks. No p-values or statistical precision superiority claim from three seeds.

Own training probes retain the separate ≥99% full-trace adequacy threshold, each length and pooled, with B L4–6 central. Low attainment does not cancel execution and makes absent transfer inconclusive about sufficient long training. Report short changes and allowed versus ADD→XOR-containing pools separately. Longer traces, semantic/compositional diversity and coverage change together; the intervention is the same training package as E36.

## Exact scientific budget

| Stage | New updates | Examples | Instruction/readout positions | Native training steps |
|---|---:|---:|---:|---:|
| Four own-parent16000→32000 extensions |64000|4096000|8192000|65536000|
| Four A8000 + four B8000 continuations |64000|4096000|11264000|90112000|
| Total new training |128000|8192000|19456000|155648000|

Both-register scalar target count is twice positions. B_match snapshots add no updates. Prior E32 training is already paid and excluded; preserve parent/new/cumulative lineage per checkpoint.

| Scientific evaluation | Forwards | Cases | Positions | Native steps |
|---|---:|---:|---:|---:|
| E37 six endpoints ×36 padding programs |216|55296|331776|2654208|
| E38 four new parent endpoints ×83 |332|48128|149248|1193984|
| E38 twelve continuation endpoints |2748|682752|4192512|33540096|
| Total |3296|786176|4673536|37388288|

Count model loads/deserializations, QA, exact independent replays, actual timing, failures and attempted/completed updates separately. Null for unmeasured time/counts, never zero. No automatic reruns. Bounded QA must exercise strict nonzero-seed parent/branch restoration and snapshot lineage, wrong-seed/parent rejection, unchanged pools and budget reconstruction; QA limits and exact actual costs are recorded before science, outside the table above.

## Integrity and STOP

Freeze protocol/source/manifest, all accepted input checkpoint and manifest hashes, parent extension streams, exact inherited pools/probes, output paths, owning task `01a08174-f596-75a2-8650-f3ccbf517fd9` and next artifact-review action before launch. Generated parent hashes are recorded when produced and must match each child's lineage. Refuse existing scientific output paths. Nonfinite/technical failure suspends affected work, preserves partials/counters and queues one failure notification; no automatic retry or unauthorized extension.

Use `scripts/run_and_wake.py` per `docs/BACKGROUND_RUNS.md`, wrapping one supervisor for the authorized group, one completion wake to the exact existing task. After confirmed detached launch end the active agent turn; no polling model turns. Artifact review resumes only on completion notification, which is not proof of success.

Independent review reconstructs DSL targets, complete row coverage, pairs, primary and attainment criteria from raw predictions; audits every parent→child model identity/optimizer step, frozen inputs, schedules and exact costs; performs one strict saved-checkpoint evaluation replay with exact prediction agreement for E37 and E38 including parent scope, no retraining. Written ACCEPT/BLOCKED refers to artifact validity separately from numerical replication outcome. After fixed artifacts and review, STOP and discuss with user regardless of effect size. No arbitrary-chain, universal algorithm, general reasoning, h64, packed-kernel or beyond-L10 claim.
